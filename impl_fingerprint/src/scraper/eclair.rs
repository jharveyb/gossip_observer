//! Stub Eclair scraper — to be implemented in a later phase.

use crate::db::VersionRecord;

/// Return all hardcoded Eclair version records.
///
/// Currently returns an empty list — Eclair data will be added in a future phase.
pub fn records() -> Vec<VersionRecord> {
    vec![]
}
